export enum EMoneda {
  PEN = 'PEN',
  USD = 'USD'
}

export enum EEstadoFactura {
  PENDIENTE = 'PENDIENTE',
  DESCONTADA = 'DESCONTADA',
  PAGADA = 'PAGADA',
  VENCIDA = 'VENCIDA'
}

export enum EPeriodo {
  MENSUAL = 30,
  BIMESTRAL = 60,
  TRIMESTRAL = 90,
  SEMESTRAL = 180,
  ANUAL = 360
}

export enum ETipoTasa {
  NOMINAL = 'NOMINAL',
  EFECTIVA = 'EFECTIVA'
}